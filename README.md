# modelcheck

Minimal validation helpers for model metadata records.

```python
from modelcheck import validate
missing = validate({"name": "demo", "version": "1"})
```

Run `pytest` to execute tests.

License: MIT. Built by meduuv. `guns.lol/meduu`
