[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "modelcheck"
version = "0.1.0"
description = "Minimal model metadata validation"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/modelcheck"]
